# Artifacts

This folder stores outputs produced by running the notebooks.

## Experiment log (main)

- `experiments.jsonl` — append-only log, **one JSON object per line** (one run = one line).
- `experiments.csv` — the same log exported as a table for PowerPoint/Excel.

These files are created/updated by `EDA.ipynb` during the final "Save experiment results" step.

